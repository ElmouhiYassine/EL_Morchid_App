<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ALMorchid" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="SpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:backgroundTint">@color/custom_spinner_background_color</item>
        <item name="android:textColor">@color/custom_spinner_text_color</item>
    </style>

    <style name="Theme.ALMorchid" parent="Base.Theme.ALMorchid" />
</resources>